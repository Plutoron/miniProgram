<!--detail.wxml-->
<view class="container">
    <image src="{{poster}}"></image>
    <text class="name">{{name}}</text>
    <text class="author">{{author}}</text>
    <text class="btn iconfont {{musicStatus ? 'icon-bofang' : 'icon-zanting'}}" catchtap="audioControl"></text>
</view>
