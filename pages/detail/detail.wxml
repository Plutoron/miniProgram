<!--detail.wxml-->
<view class="container">
    <image src="{{poster}}"></image>
    <text class="name">{{name}}</text>
    <text class="author">{{author}}</text>
    <text class="btn iconfont {{musicStatus == 'play' ? 'icon-bofang' : 'icon-zanting'}}" bindtap="audioControl"></text>
</view>
